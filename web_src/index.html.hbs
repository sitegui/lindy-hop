<!DOCTYPE html>
<html lang="fr">
<head>
    {{> head base_url="." title="Vidéos Lindy Hop"}}
    <script type="module">
        window.applyFilter('')
    </script>
</head>
<body>
<h1 class="page-title">Vidéos Lindy Hop</h1>

Montrer vidéos de <select id="tag-filter" onchange="applyFilter(this.value)">
    <option value="">(tout)</option>
    {{#each all_tags}}
        <option value="{{tag}}">{{tag}} ({{count}})</option>
    {{/each}}
</select>

<div>
    {{#each videos}}
        {{> video base_url="." access_salt=../access_salt access_iterations=../access_iterations}}
    {{/each}}
</div>
</body>
</html>